# 일부러 틀린 표본 100개 검사 보고서

v3.2 계약은 독립적인 잘못된 표본 100개와 100% 거부를 요구한다. 이번 실행에서는 100개를 실제 주입·실행하지 않았으므로 `NOT_RUN`이다.

대신 다음 10개 오류군을 정의했다: 잘못된 합계, 미래 날짜 역전, 음수 인원, 허용되지 않은 URL, OAuth state 불일치, 만료 세션, 세금 누락, 통화 불일치, stale 가격 미표시, 표본을 실시간으로 표시. 다음 실행에서는 각 오류군을 10개씩 생성해 모두 거부되는지 기록한다.

현재 탐지율: 산출하지 않음. 이 파일은 X10 완료 증거가 아니다.
