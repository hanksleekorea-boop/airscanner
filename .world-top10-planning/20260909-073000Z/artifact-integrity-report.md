# 결과물 무결성 보고서

- 이전 v3.0 실행 산출물은 `20260909-054409Z`에 보존했고, v3.2 확장 산출물은 `20260909-073000Z`에 별도 생성했다.
- v3.2 확장 JSON 10개와 기존 재사용 JSON을 PowerShell `ConvertFrom-Json`으로 확인했다: `JSON_PARSE=PASS`.
- `x10-workload-ledger.json`은 각 차원의 기준·목표·실적·중복·미확인·배수·대체작업·근거 ID를 포함한다.
- 필수 v3.2 파일 27개가 존재한다. 10배 기준 미달 차원은 숨기지 않고 `X10_PARTIAL_SCOPE`로 기록했다.
- 내부 링크는 같은 실행 폴더와 이전 실행 폴더를 명시하며, 비밀값·토큰·쿠키 원문은 없다.
- 제품 코드·Git·Cloudflare·공개 서비스 변경은 이 실행에서 0건이다.
